## 📚 E-BOOK: JAVA PARA INICIANTES 🧪

Este repositório contém o e-book **"Java para Iniciantes: Fundamentos da Programação e Conformidade com Normas Técnicas"**, um guia didático e progressivo para introduzir profissionais aos conceitos essenciais da linguagem Java.

O material aborda desde a configuração do ambiente até a Orientação a Objetos, com foco na aplicação prática e no entendimento dos mecanismos centrais da linguagem.

---

### 🎯 FOCO PRINCIPAL DO CONTEÚDO

O e-book é organizado para construir o conhecimento do leitor de forma estruturada:

* **Fundamentos e Configuração:** Entendimento da arquitetura Java (WORA) e dos componentes essenciais: **JDK**, **JRE** e **JVM**. Inclui passos detalhados para instalação e configuração de **variáveis de ambiente** (`JAVA_HOME` e `PATH`).
* **Sintaxe Básica:** Estrutura mínima de um código, processo de **compilação e execução**, e o entendimento do método **`main()`** como ponto de entrada obrigatório da aplicação.
* **Tipagem e Operadores:** Java é uma linguagem **estaticamente tipada**. Explica a diferença entre os **8 tipos de dados primitivos** e os tipos de referência (como `String`), além dos operadores essenciais (aritméticos, de comparação e lógicos).
* **Controle de Fluxo:** Uso prático de estruturas condicionais (`if`, `else`, `switch`) e estruturas de repetição (loops `for`, `while`, `do-while`).
* **Estruturas de Dados:** Transição de estruturas **estáticas** (`Arrays`) para **dinâmicas** (`ArrayList`).

---

### 🔑 INTRODUÇÃO À ORIENTAÇÃO A OBJETOS (OO)

A seção de Orientação a Objetos é fundamental, introduzindo os conceitos indispensáveis para o desenvolvimento em Java:

* **Classes e Objetos:** A Classe como o "molde" ou planta baixa, e o Objeto como a "instância concreta".
* **Atributos e Métodos:** Diferenciação entre Atributos (estado) e Métodos (comportamento) de um objeto.
* **Encapsulamento:** Pilar da OO que protege o estado interno do objeto, utilizando os métodos **Getters** e **Setters** para controlar o acesso aos dados `private`.

---

### 📊 TABELAS DE REFERÊNCIA

#### Tipos de Dados Primitivos em Java

| Tipo de Dado | Tamanho (bits) | Descrição | Valor Mínimo/Máximo |
| :--- | :--- | :--- | :--- |
| **byte** | 8 | Inteiro pequeno | $-128~a~127$ |
| **short** | 16 | Inteiro curto | $-32.768~a~32.767$ |
| **int** | 32 | Inteiro padrão (mais comum) | $\pm$ 2 bilhões |
| **long** | 64 | Inteiro longo | Valores muito grandes |
| **float** | 32 | Ponto flutuante de precisão simples | (Decimal) |
| **double** | 64 | Ponto flutuante de precisão dupla (padrão) | (Decimal de alta precisão) |
| **char** | 16 | Caractere Unicode | (Caractere único) |
| **boolean** | 1 | Valor lógico | `true` ou `false` |

#### Modificadores de Acesso em Java (Visibilidade)

| Modificador | Mesma Classe | Mesmo Pacote | Pacotes Diferentes |
| :--- | :--- | :--- | :--- |
| `private` | Sim | Não | Não |
| `public` | Sim | Sim | Sim |

---

### 🗺️ PRÓXIMOS PASSOS NA JORNADA JAVA

Para o aprofundamento do conhecimento, o leitor é incentivado a explorar os seguintes tópicos de forma progressiva:

1.  Pilares Avançados de OO: Herança, Polimorfismo e Abstração.
2.  Tratamento de Exceções: Uso de `try-catch` para lidar com erros em tempo de execução.
3.
